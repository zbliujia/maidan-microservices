# Spring properties
spring:
  freemarker:
    enabled: false     # Ignore Eureka dashboard FreeMarker templates
  http:
    encoding.charset: UTF-8
    encoding.enable: true
    encoding.force: true
  redis:
    host: 127.0.0.1
    port: 6379

logging:
  file: wechat_server.log
# HTTP Server
server:
  port: 0   # HTTP (Tomcat) port
